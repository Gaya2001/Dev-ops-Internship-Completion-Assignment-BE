apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: kavindu
  annotations:
    cloud.google.com/backend-config: '{"default": "backend-health-check"}'
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer
